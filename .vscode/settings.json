{
    "sqltools.connections": [
        {
            "previewLimit": 50,
            "server": "localhost",
            "driver": "PostgreSQL",
            "name": "DB",
            "connectString": "postgres://postgres:postgres@localhost:5432/data?sslmode=disable"
        }
    ]
}